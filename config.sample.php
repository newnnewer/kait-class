<?php
/* ═══════════════════════════════════════════════
   config.sample.php — 설치 환경에 맞춰 고치는 값들

   이 파일을 config.php 로 복사해서 쓴다.

       cp config.sample.php config.php

   config.php 는 설치할 때마다 달라지므로 저장소에 올리지 않는다.
   프로그램 동작을 정하는 값(제출 간격, 차이 보기 길이 등)은 여기가 아니라
   코드에 있다. 여기 있는 것은 "이 서버에서는 어디에 무엇이 있는가" 뿐이다.

   사이트 이름·로고·회원가입 허용 같은 것은 이 파일이 아니라
   웹 화면(운영 > 설정)에서 바꾼다.
   ═══════════════════════════════════════════════ */

return [

  /* ── DB 파일 ───────────────────────────────────
     ★ 반드시 문서 루트 바깥에 두어야 한다.
     문서 루트 안에 있으면 웹 서버 설정 한 줄만 빠져도
     http://…/data/oj.db 로 전체 계정과 비밀번호 해시가 통째로 내려받힌다.

     기본값은 문서 루트 바로 위의 kait-class-data/ 폴더다.
     문서 루트가 /var/www/html 이면 /var/www/kait-class-data/kait-class.db 가 된다.
     이 폴더는 웹 서버 사용자(보통 www-data)가 읽고 쓸 수 있어야 한다. */
  'db_path'  => dirname(__DIR__) . '/kait-class-data/kait-class.db',

  /* PHP 오류 기록. 보통 DB 와 같은 폴더에 둔다. */
  'log_path' => dirname(__DIR__) . '/kait-class-data/php-error.log',

  /* ── 채점 엔진 ─────────────────────────────────
     Judge0 주소. 같은 기계에 설치했다면 이대로 두면 된다.
     바깥에 열지 말고 127.0.0.1 로만 받는 것이 안전하다. */
  'judge0_url' => 'http://127.0.0.1:2358',

  /* ── 시간대 ───────────────────────────────────
     수업·평가의 시작·종료 시각을 이 기준으로 판단한다.
     목록: https://www.php.net/manual/en/timezones.php */
  'timezone' => 'Asia/Seoul',

];
