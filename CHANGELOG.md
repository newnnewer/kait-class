# 변경 기록

이 프로젝트의 주요 변경 사항을 버전별로 적습니다.
버전 번호는 [유의적 버전](https://semver.org/lang/ko/)(주.부.수)을 따릅니다.

- **주** — 크게 바뀌어 이전 버전과 맞지 않을 때
- **부** — 기능이 추가될 때
- **수** — 버그만 고칠 때

## [미공개]

## [1.1.0] - 2026-09-18

### 수업·평가
- 목록 차례를 **끝나는 날이 늦은 순**으로 바꿈. 끝나는 날을 정하지 않은 것이 맨 위,
  같으면 나중에 만든 것이 위. 학생 화면과 관리자 화면이 같은 차례를 쓴다

### 회원 관리
- 이름 바꾸기 (아이디는 바꾸지 않는다)
- 선택한 학생 일괄 삭제. 되돌릴 수 없어 확인 창에 `삭제` 를 직접 입력해야 진행된다.
  제출 기록도 함께 사라진다

### 문제 관리
- 선택한 문제 삭제. 확인 창에 번호와 함께 지워질 제출 기록 수를 보여 주고,
  `삭제` 를 직접 입력해야 진행된다
- **제출 기록이 있어도 삭제할 수 있게 됨** (제출 기록을 문제와 함께 지운다).
  수업·평가에 들어 있는 문제는 여전히 지울 수 없다 — 하나라도 있으면 아무것도 지우지 않는다
- 선택한 문제로 **수업 만들기 · 평가 만들기**. 고른 번호를 편집 화면의 '문제 번호로 추가'에 담아 주며,
  이름·기간·공개 대상을 정해 저장할 때 만들어진다 (중간에 그만두어도 빈 묶음이 남지 않는다)

## [1.0.0] - 2026-09-17

첫 공개 버전입니다.

### 설치
- 한 줄 설치 `get.sh` — 최신 릴리스를 홈 폴더에 내려받아 설치를 시작
  (`wget -qO- https://raw.githubusercontent.com/newnnewer/kait-class/main/get.sh | sudo bash`)
- 설치가 끝나면 관리자 비밀번호를 정하는 명령을 안내

### 보안
- 첫 관리자 계정에 정해 둔 비밀번호를 없앰. 처음 만들 때 무작위 값을 넣고,
  설치한 사람이 `reset-admin.php` 로 직접 정함 (소스가 공개되므로)

### 문서
- `README.md` — 소개, 필요한 것, 설치, 도메인·https, 업데이트, 문제 해결, 이용 조건
- 파일별 설명을 `README-소스구성.txt` 에서 `docs/소스구성.md` 로 옮김
- 이용 조건 안내에 "학교가 운영하는 방과후학교는 수익자부담이어도 사용 가능"을 적음
- `.gitattributes` — 윈도우에서 올려도 줄바꿈이 LF 로 유지되게 함

## [0.9.2] - 2026-09-17

### 설치 스크립트
- 도메인과 https(SSL) 붙이기: `sudo bash install.sh --domain 도메인 --email 메일`
  - Let's Encrypt 무료 인증서를 받고, 우분투가 자동으로 갱신 (갱신되면 nginx 가 새 인증서를 읽음)
  - 도메인을 기억해 두어, 다음 업데이트부터는 옵션 없이 실행해도 https 가 유지됨
  - 바깥에서 http 로 들어오면 https 로 넘김. 공유기 안에서 IP 주소로 들어오면 http 그대로 열림
  - 도메인 연결·포트포워딩이 안 되어 인증서를 못 받으면 설치를 멈추지 않고 http 로 계속하며, 원인을 알려 줌
  - `--no-domain` 으로 http 로 되돌리기, `--help` 로 사용법 보기
  - 단계가 13개에서 14개로 (12단계 "도메인 · SSL")
- 메모리 경고 문구를 "100명 이상이 동시에 참가하는 대회를 열려면 4GB 이상을 권합니다"로 바꿈

### 화면
- 첫 화면의 위아래 여백을 줄여 1920×1080 화면에서 스크롤 없이 보이게 함
  (로그인 전 첫 화면의 공지는 4개까지, 로그인 뒤에는 지금처럼 6개까지)

### 보안
- https 로 접속하면 로그인 쿠키를 https 에서만 보내도록 함

## [0.9.1] - 2026-09-17

클라우드(iwinv, 2GB) 설치 시험에서 찾은 것을 고쳤습니다.

### 설치 스크립트
- 소스가 `/tmp` 같은 임시 폴더에 있으면 1단계에서 멈추고 홈 폴더로 옮기라고 안내
  (재부팅하면 지워져 설치를 이어갈 수 없었음)
- 설치가 끝난 뒤 안내를 내부 IP(공유기 안)와 공인 IP(클라우드)로 나눔
- 설치 기록에 단계별 걸린 시간과 설치 끝 시각을 남김

## [0.9.0] - 2026-09-17

기능 개발을 마친 첫 시험판입니다. 실제 운영으로 확인한 뒤 1.0.0 으로 정식 공개합니다.

### 기능
- 코드를 직접 입력해 푸는 문제 풀이, Judge0 채점 (접수와 채점 분리, 상주 채점 워커)
- 수업·평가 묶음 — 반별/전체 공개, 기간, 붙여넣기 차단, 평가의 통과 횟수 조건
- "다음 회차 시작" 때 입력란과 되돌리기 기록을 함께 비움
- 진행·완료 표시 (완료 / 참여 중)
- 틀린 경우 첫 오답 케이스의 입력·기대 출력·내 출력 보기 (끌 수 있음)
- 직접 실행, 채점 결과 목록, 학생 활동
- 공지사항 (마크다운, 이미지 올리기)
- 문제 관리 — 테스트케이스, 출처·분류, FPS XML 내보내기·가져오기, 번호 일괄 수정
- 회원 — CSV 일괄 등록, 반 관리, 학교 밖 회원가입(승인제, 기본 꺼짐), 계정 단위 로그인 잠금
- 운영 — 채점 상태, 언어 관리, 사이트 이름·로고·꼬리말 설정
- 설치·업데이트 스크립트 `install.sh` (Ubuntu 24.04, x86_64)

### 라이선스
- PolyForm Noncommercial 1.0.0. 한국정보교사연합회(KAIT)와 문광식의 공동 저작물
- 꼬리말 저작권 표기: 실명 대신 아이디 newnnewer (메일 링크)
